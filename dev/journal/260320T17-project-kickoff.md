# Project Kickoff: ralph-wiggum-asset-pricing
**Date:** 2026-03-20, 5:00 PM ET

## Plan

Bootstrapping this project by bringing over the Ralph architecture from the
previous repo at `./dev/HumanxAI-ChenAY/`.

### Steps

1. Copy `CLAUDE.md` from `./dev/HumanxAI-ChenAY/` to project root — **done**.
2. Adapt `CLAUDE.md` for this asset-pricing project (trim irrelevant sections,
   update folder structure).
3. Copy over the Ralph folder structure (`ralph/`, `ralph-garage/`, `spec/`, etc.)
   as needed.
4. Set up `spec/paper-spec.md` and other specification files for the new project.
5. Adapt ralph loop tooling (`ralph/tools/`) to this project's needs.

### Source Attribution

The Ralph architecture originates from `./dev/HumanxAI-ChenAY/`. That directory
is kept in-tree as a reference for the original repo structure and code.
